domain test.domain01

import shared.sharedDomain
include "shared/model01.model"

id TestValIdentifier {
  userId: UserId
}

id TestIdentifer {
  userId: UserId
  context: str
}

mixin TestInterface {
  userId: UserId
  accountBalance: i32
  latestLogin: i64
  keys: map[str, str]
  nicknames: list[str]
}

data TestObject {
  + TestInterface
}

service UserService {
  def createUser(TestInterface): (TestInterface)
}
