package izumi.distage.testkit.distagesuite.compiletime

// Stubbed in M5/11c — relies on StaticTestMain plugin fixtures whose effect type doesn't match
// the bifunctorized framework's RoleService[F[+_, +_]] shape.
