ListCaseClass.mylist:
    [
      [
        {
          wrap {
            port = 80
            host = "localhost"
          }
        },

        {
          wrap {
            port = 8080
            host = "localhost"
          }
        },

        {
          wrap {
            port = 8888
            host = "localhost"
          }
        }
      ]
    ]
