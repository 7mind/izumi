Listener {
  HostPort {
    service1 {
      port = 80
      host = "localhost"
    }

    service2 {
      port = 8080
      host = "localhost"
    }

    % {
      port = 8888
      host = "localhost"
    }

  }
}

cassandra {
  HostPort {
    % {
      port = 9000
      host = "cassandra"
    }
  }
}
