Endpoint {
  service1 {
    HostPort {
      port = 80
      host = "localhost"
    }
  }

  HostPort {
    port = 8888
    host = "localhost"
  }
}

Endpoint.service2 {
  HostPort.address {
    port = 8080
    host = "localhost"
  }
}

cassandra {
  HostPort.cassAddress {
    port = 9000
    host = "cassandra"
  }
}

absolute.path {
  port = 80
  host = "google.com"
}
