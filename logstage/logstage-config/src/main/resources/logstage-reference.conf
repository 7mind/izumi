// here you need to define all rendering policies you want to use
// @id is to identify policy id for it's extending or using in sinks
// @path is a full RenderingPolicy class name
// @params is a map of parameters to set a rendering policy constructor
renderingPolicies = [
  {
    id = "default"
    path = """com.github.pshirshov.izumi.logstage.api.rendering.StringRenderingPolicy"""
    params = {
      withColors = true
      withExceptions = true
      pattern = """$${level} $${ts} $${thread} $${location} $${custom-ctx} $${msg}"""
    }
  },
  {
    id = "default-json"
    path = """com.github.pshirshov.izumi.logstage.api.rendering.json.JsonRenderingPolicy"""
    params = {
      prettyPrint = true
    }
  }
]

// here you need to define all sinks you want to use
// @id is to identify policy id for it's extending or using in sinks
// @path is a full RenderingPolicy class name
// @policy is an identity of recently defined policies
// @params is a map of parameters to set a rendering policy constructor
//sinks = [
//  {
//    id = "default"
//    path = """com.github.pshirshov.izumi.logstage.sink.console.ConsoleSink"""
//    policy = "default"
//    params {
//
//    }
//  }
//]


// defines logging settings for root package
// @threshold defines logging level
// @sinks defines enumeration of sinks recently defines in @sinks section
//default {
//  threshold = "info"
//  sinks = [
////    "default"
//  ]
//}

// defines logging settings for each package
// @threshold defines logging level
// @sinks defines enumeration of sinks recently defines in @sinks section
// NOTE! you may define only log level for package. It means that you will use all sinks defined in default section
// i.e.

// default {
//    threshold = "info"
//    sinks = [
//        "s1", "s2"
//    ]
// }
// entries = {
//    "pkg1" = {
//      threshold = "debug"
//      sinks = [
//        "s1"
//      ]
//    }
//    "pkg2" = "trace"
// }
// it means that  for pkg1 sinks are only s1 but for pkg2 by default sinks list will contain both s1 and s2


//entries = {
//  "com.github.pshirshov.*" = "trace"
//  "com.typesafe" = {
//    threshold = "debug"
//    sinks = [
//      "default"
//    ]
//  }
//}
